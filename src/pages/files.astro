---
import Layout from '../layouts/Layout.astro';

// 数据源
const fileLinks = [
  { name: "常用软件合集 (Win/Mac)", url: "#", desc: "提取码: 1234 | v2025.1", type: "zip" },
  { name: "前端学习路线图.pdf", url: "#", desc: "高清矢量图", type: "pdf" },
  { name: "个人项目源码备份", url: "#", desc: "GitHub 私有库镜像", type: "code" },
  { name: "设计素材包", url: "#", desc: "字体、图标、配色方案", type: "folder" },
];

// 简单的图标映射 helper
const getIcon = (type) => {
  const icons = { zip: '📦', pdf: '📄', code: '💻', folder: '📂', default: '🔗' };
  return icons[type] || icons.default;
};
---

<Layout title="资源汇总 | Files">
  <div class="tech-bg"></div>

  <main class="container">
    <div class="header-box">
      <h1>资源归档 <span>/ FILES</span></h1>
      <p>精选工具、文档与网盘资源直链。</p>
    </div>
    
    <div class="file-grid">
      {fileLinks.map((file) => (
        <div class="file-item">
          <div class="icon-wrapper">{getIcon(file.type)}</div>
          <div class="file-info">
            <h3>{file.name}</h3>
            <p class="tag">{file.desc}</p>
          </div>
          <a href={file.url} target="_blank" class="download-btn">
            Download
          </a>
        </div>
      ))}
    </div>
  </main>
</Layout>

<style>
  /* 科技感微噪点背景 */
  .tech-bg {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;
    background-color: #f3f4f6;
    background-image: linear-gradient(0deg, transparent 24%, rgba(0, 0, 0, .05) 25%, rgba(0, 0, 0, .05) 26%, transparent 27%, transparent 74%, rgba(0, 0,
